@model decimal?
@if (ViewData.ModelMetadata.IsReadOnly)
{
    @(Html.Kendo().NumericTextBoxFor(m => m)
          .HtmlAttributes(new { @readonly = "readonly", @class = "readonly" }))
}
else
{
    @(Html.Kendo().NumericTextBoxFor(m => m))
}