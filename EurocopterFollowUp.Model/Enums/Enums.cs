namespace EurocopterFollowUp.Model.Enums
{
    public class Enums
    {
        public enum DisplayType
        {
            AllItems = 1,
            MyItems = 2
        }

        public enum RoleNames
        {
            Administrator = 1,
            Manager = 2,
            User = 3
        }

        public enum MessageType
        {
            Information = 0,
            Warning,
            Error,
            Success
        }
    }
}